import { NavLink, Link, Navigate } from 'react-router-dom';
export const Home = () => {
  return (
    <nav>
      <ul>
        <li>
          <NavLink to="/"> Home</NavLink>
        </li>
        <li>
          <NavLink to="/"> Home</NavLink>
        </li>
      </ul>
    </nav>
  );
};
